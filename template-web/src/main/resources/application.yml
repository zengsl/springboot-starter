spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/res_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=Asia/Shanghai
    username: root
    password: root


mybatis-plus:
  mapperLocations: classpath*:/mapper/**/*.xml
  typeAliasesPackage: com.ddp.res.pojo
  globalConfig:
    dbConfig:
      idType: AUTO

server:
  shutdown: "graceful"
  servlet:
    register-default-servlet: true

